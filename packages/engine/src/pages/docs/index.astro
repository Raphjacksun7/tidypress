---
import { getCollection } from 'astro:content'

const docs = (await getCollection('docs'))
  .sort((a, b) => (a.data.order ?? 99) - (b.data.order ?? 99))

const first = docs[0]
if (first) {
  return Astro.redirect(`/docs/${first.id}`, 301)
}
---
